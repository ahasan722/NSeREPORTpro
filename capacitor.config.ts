import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.nscxb.nserpro',
  appName: 'NSeREPORTpro',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
